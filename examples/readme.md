# Examples

example1 should be
```
    green regardless of flags
```

example2 should be
```
    green for     flag -l
    red   without flag -l
```

example3 should be
```
    green for     flag -s
    green without flag -s
```